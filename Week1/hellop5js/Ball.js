
class Ball
{
constructor(x, y)
{
  this.x = x;
  this.y = y;
  this.c = color((random(255),random(255),random(255),random(255));
  this.r = random(5,50);

}

display()
{
  fill(c);
  ellipse(this.x, this.y, this.r, this.r);
}

}


class Ball
{
constructor(x, y)
{
  this.x = x;
  this.y = y;
  c = color((random(255),random(255),random(255),random(255));
  r = random(5,50);

}

display()
{
  fill(c);
  ellipse(this.x, this.y, r,r);
}

}
